plugins {
    id 'java'
}

allprojects {
    group = 'io.github.configstackmc'
    version = '1.0.0'
}

// Fabric Loom 1.17 requires Gradle 9.5; gradlew reads that version from gradle-wrapper.properties.
def targetJavaVersion = (findProperty('javaVersion') ?: '25') as int

subprojects {
    apply plugin: 'java'

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(targetJavaVersion)
        }
    }

    tasks.withType(JavaCompile).configureEach {
        options.encoding = 'UTF-8'
        options.release = targetJavaVersion
    }

    tasks.withType(Test).configureEach {
        useJUnitPlatform()
    }
}
